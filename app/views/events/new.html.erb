<h1>New event</h1>
<%= form_for(@event) do |f| %>
  <%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.label :date %>
	<%= f.date_select :date %>

	<%= f.submit "Create" %>
<% end %>